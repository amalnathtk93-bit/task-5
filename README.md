# task-5
Using Wireshark, I captured live network traffic from my active interface while browsing websites and using ping. I analyzed packets using filters such as DNS, TCP, ICMP, and TLS, identified key protocols, examined packet details, and exported the capture as a .pcap file for reporting.
